-- hello guys this is the trade scam ui 

local Players = game:GetService("Players")
local TweenService = game:GetService("TweenService")
local UserInputService = game:GetService("UserInputService")
local GuiService = game:GetService("GuiService")

local player = Players.LocalPlayer
local playerGui = player:WaitForChild("PlayerGui")

-- Detect if mobile
local isMobile = UserInputService.TouchEnabled and not UserInputService.KeyboardEnabled

-- Create ScreenGui
local screenGui = Instance.new("ScreenGui")
screenGui.Name = "TradeScamGUI"
screenGui.Parent = playerGui

-- Main Frame (App Window)
local mainFrame = Instance.new("Frame")
mainFrame.Name = "MainFrame"
-- Responsive sizing based on device
if isMobile then
    mainFrame.Size = UDim2.new(0, 320, 0, 240)
    mainFrame.Position = UDim2.new(0.5, -160, 0.5, -120)
else
    mainFrame.Size = UDim2.new(0, 400, 0, 280)
    mainFrame.Position = UDim2.new(0.5, -200, 0.5, -140)
end
mainFrame.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
mainFrame.BorderSizePixel = 0
mainFrame.Parent = screenGui

-- Add corner radius
local mainCorner = Instance.new("UICorner")
mainCorner.CornerRadius = UDim.new(0, 20)
mainCorner.Parent = mainFrame

-- Title Bar
local titleBar = Instance.new("Frame")
titleBar.Name = "TitleBar"
titleBar.Size = UDim2.new(1, 0, 0, 55)
titleBar.Position = UDim2.new(0, 0, 0, 0)
titleBar.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
titleBar.BorderSizePixel = 0
titleBar.Parent = mainFrame

-- Add title bar corner radius
local titleCorner = Instance.new("UICorner")
titleCorner.CornerRadius = UDim.new(0, 20)
titleCorner.Parent = titleBar

-- Title Text
local titleLabel = Instance.new("TextLabel")
titleLabel.Name = "Title"
titleLabel.Size = UDim2.new(0.7, 0, 0.65, 0)
titleLabel.Position = UDim2.new(0, 20, 0, 5)
titleLabel.BackgroundTransparency = 1
titleLabel.Text = "Trade Scam Gag"
titleLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
-- Responsive text sizing
if isMobile then
    titleLabel.TextSize = 16
else
    titleLabel.TextSize = 18
end
titleLabel.TextXAlignment = Enum.TextXAlignment.Left
titleLabel.Font = Enum.Font.GothamBold
titleLabel.Parent = titleBar

-- Version Label
local versionLabel = Instance.new("TextLabel")
versionLabel.Name = "Version"
versionLabel.Size = UDim2.new(0.7, 0, 0.35, 0)
versionLabel.Position = UDim2.new(0, 20, 0.65, 0)
versionLabel.BackgroundTransparency = 1
versionLabel.Text = "Version 2.1.0"
versionLabel.TextColor3 = Color3.fromRGB(136, 136, 136)
-- Responsive text sizing
if isMobile then
    versionLabel.TextSize = 10
else
    versionLabel.TextSize = 12
end
versionLabel.TextXAlignment = Enum.TextXAlignment.Left
versionLabel.Font = Enum.Font.Gotham
versionLabel.Parent = titleBar

-- Window Controls Container
local controlsFrame = Instance.new("Frame")
controlsFrame.Name = "Controls"
controlsFrame.Size = UDim2.new(0, 50, 0, 20)
controlsFrame.Position = UDim2.new(1, -60, 0, 15)
controlsFrame.BackgroundTransparency = 1
controlsFrame.Parent = titleBar

-- Minimize Button (-)
local minimizeBtn = Instance.new("TextButton")
minimizeBtn.Name = "MinimizeButton"
minimizeBtn.Size = UDim2.new(0, 20, 0, 20)
minimizeBtn.Position = UDim2.new(0, 0, 0, 0)
minimizeBtn.BackgroundColor3 = Color3.fromRGB(255, 189, 46)
minimizeBtn.BorderSizePixel = 0
minimizeBtn.Text = "─"
minimizeBtn.TextColor3 = Color3.fromRGB(0, 0, 0)
minimizeBtn.TextSize = 14
minimizeBtn.Font = Enum.Font.GothamBold
minimizeBtn.Parent = controlsFrame

local minimizeCorner = Instance.new("UICorner")
minimizeCorner.CornerRadius = UDim.new(0.5, 0)
minimizeCorner.Parent = minimizeBtn

-- Close Button (X)
local closeBtn = Instance.new("TextButton")
closeBtn.Name = "CloseButton"
closeBtn.Size = UDim2.new(0, 20, 0, 20)
closeBtn.Position = UDim2.new(0, 25, 0, 0)
closeBtn.BackgroundColor3 = Color3.fromRGB(255, 95, 87)
closeBtn.BorderSizePixel = 0
closeBtn.Text = "X"
closeBtn.TextColor3 = Color3.fromRGB(0, 0, 0)
closeBtn.TextSize = 12
closeBtn.Font = Enum.Font.GothamBold
closeBtn.Parent = controlsFrame

local closeCorner = Instance.new("UICorner")
closeCorner.CornerRadius = UDim.new(0.5, 0)
closeCorner.Parent = closeBtn

-- Divider Line
local dividerLine = Instance.new("Frame")
dividerLine.Name = "Divider"
dividerLine.Size = UDim2.new(1, -40, 0, 1)
dividerLine.Position = UDim2.new(0, 20, 0, 55)
dividerLine.BackgroundColor3 = Color3.fromRGB(40, 40, 40)
dividerLine.BorderSizePixel = 0
dividerLine.Parent = mainFrame

-- Content Area
local contentFrame = Instance.new("Frame")
contentFrame.Name = "Content"
contentFrame.Size = UDim2.new(1, -40, 1, -80)
contentFrame.Position = UDim2.new(0, 20, 0, 70)
contentFrame.BackgroundTransparency = 1
contentFrame.Parent = mainFrame

-- Section Title
local sectionTitle = Instance.new("TextLabel")
sectionTitle.Name = "SectionTitle"
sectionTitle.Size = UDim2.new(1, 0, 0, 22)
sectionTitle.Position = UDim2.new(0, 0, 0, 0)
sectionTitle.BackgroundTransparency = 1
sectionTitle.Text = "Name Input"
sectionTitle.TextColor3 = Color3.fromRGB(255, 255, 255)
-- Responsive text sizing
if isMobile then
    sectionTitle.TextSize = 14
else
    sectionTitle.TextSize = 16
end
sectionTitle.TextXAlignment = Enum.TextXAlignment.Left
sectionTitle.Font = Enum.Font.GothamBold
sectionTitle.Parent = contentFrame

-- Section Subtitle
local sectionSubtitle = Instance.new("TextLabel")
sectionSubtitle.Name = "SectionSubtitle"
sectionSubtitle.Size = UDim2.new(1, 0, 0, 18)
sectionSubtitle.Position = UDim2.new(0, 0, 0, 22)
sectionSubtitle.BackgroundTransparency = 1
sectionSubtitle.Text = "Enter"
sectionSubtitle.TextColor3 = Color3.fromRGB(136, 136, 136)
-- Responsive text sizing
if isMobile then
    sectionSubtitle.TextSize = 10
else
    sectionSubtitle.TextSize = 12
end
sectionSubtitle.TextXAlignment = Enum.TextXAlignment.Left
sectionSubtitle.Font = Enum.Font.Gotham
sectionSubtitle.Parent = contentFrame

-- Name Input TextBox
local nameInput = Instance.new("TextBox")
nameInput.Name = "NameInput"
nameInput.Size = UDim2.new(1, 0, 0, 35)
nameInput.Position = UDim2.new(0, 0, 0, 50)
nameInput.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
nameInput.BorderSizePixel = 0
nameInput.Text = ""
nameInput.PlaceholderText = "Enter Name to process..."
nameInput.TextColor3 = Color3.fromRGB(255, 255, 255)
nameInput.PlaceholderColor3 = Color3.fromRGB(136, 136, 136)
-- Responsive text sizing
if isMobile then
    nameInput.TextSize = 12
else
    nameInput.TextSize = 14
end
nameInput.Font = Enum.Font.Gotham
nameInput.Parent = contentFrame

local inputCorner = Instance.new("UICorner")
inputCorner.CornerRadius = UDim.new(0, 12)
inputCorner.Parent = nameInput

-- Freeze Trade Button
local freezeBtn = Instance.new("TextButton")
freezeBtn.Name = "FreezeButton"
freezeBtn.Size = UDim2.new(0.48, 0, 0, 35)
freezeBtn.Position = UDim2.new(0, 0, 0, 100)
freezeBtn.BackgroundColor3 = Color3.fromRGB(0, 122, 255)
freezeBtn.BorderSizePixel = 0
freezeBtn.Text = "Freeze Trade"
freezeBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
-- Responsive text sizing
if isMobile then
    freezeBtn.TextSize = 12
else
    freezeBtn.TextSize = 14
end
freezeBtn.Font = Enum.Font.GothamBold
freezeBtn.Parent = contentFrame

local freezeCorner = Instance.new("UICorner")
freezeCorner.CornerRadius = UDim.new(0, 12)
freezeCorner.Parent = freezeBtn

-- Force Accept Button
local forceAcceptBtn = Instance.new("TextButton")
forceAcceptBtn.Name = "ForceAcceptButton"
forceAcceptBtn.Size = UDim2.new(0.48, 0, 0, 35)
forceAcceptBtn.Position = UDim2.new(0.52, 0, 0, 100)
forceAcceptBtn.BackgroundColor3 = Color3.fromRGB(40, 167, 69)
forceAcceptBtn.BorderSizePixel = 0
forceAcceptBtn.Text = "Force Accept"
forceAcceptBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
-- Responsive text sizing
if isMobile then
    forceAcceptBtn.TextSize = 12
else
    forceAcceptBtn.TextSize = 14
end
forceAcceptBtn.Font = Enum.Font.GothamBold
forceAcceptBtn.Parent = contentFrame

local acceptCorner = Instance.new("UICorner")
acceptCorner.CornerRadius = UDim.new(0, 12)
acceptCorner.Parent = forceAcceptBtn

-- Notification Frame
local notificationFrame = Instance.new("Frame")
notificationFrame.Name = "Notification"
-- Responsive notification sizing
if isMobile then
    notificationFrame.Size = UDim2.new(0, 240, 0, 60)
    notificationFrame.Position = UDim2.new(1, -260, 0, 20)
else
    notificationFrame.Size = UDim2.new(0, 280, 0, 70)
    notificationFrame.Position = UDim2.new(1, -300, 0, 20)
end
notificationFrame.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
notificationFrame.BorderSizePixel = 0
notificationFrame.Visible = false
notificationFrame.Parent = screenGui

local notifCorner = Instance.new("UICorner")
notifCorner.CornerRadius = UDim.new(0, 15)
notifCorner.Parent = notificationFrame

-- Notification Title
local notifTitle = Instance.new("TextLabel")
notifTitle.Name = "NotificationTitle"
notifTitle.Size = UDim2.new(1, -20, 0, 22)
notifTitle.Position = UDim2.new(0, 10, 0, 8)
notifTitle.BackgroundTransparency = 1
notifTitle.Text = "Service Status"
notifTitle.TextColor3 = Color3.fromRGB(255, 255, 255)
-- Responsive text sizing
if isMobile then
    notifTitle.TextSize = 12
else
    notifTitle.TextSize = 14
end
notifTitle.TextXAlignment = Enum.TextXAlignment.Left
notifTitle.Font = Enum.Font.GothamBold
notifTitle.Parent = notificationFrame

-- Notification Message
local notifMessage = Instance.new("TextLabel")
notifMessage.Name = "NotificationMessage"
notifMessage.Size = UDim2.new(1, -20, 0, 30)
notifMessage.Position = UDim2.new(0, 10, 0, 30)
notifMessage.BackgroundTransparency = 1
notifMessage.Text = "Please wait 3 minutes in the server. If it's not working, try again."
notifMessage.TextColor3 = Color3.fromRGB(180, 180, 180)
-- Responsive text sizing
if isMobile then
    notifMessage.TextSize = 9
else
    notifMessage.TextSize = 11
end
notifMessage.TextXAlignment = Enum.TextXAlignment.Left
notifMessage.TextWrapped = true
notifMessage.Font = Enum.Font.Gotham
notifMessage.Parent = notificationFrame

-- Functions
local function showNotification()
    notificationFrame.Visible = true
    local tweenInfo = TweenInfo.new(0.3, Enum.EasingStyle.Quart, Enum.EasingDirection.Out)
    -- Responsive notification positioning
    local targetPos = isMobile and UDim2.new(1, -260, 0, 20) or UDim2.new(1, -300, 0, 20)
    local hidePos = isMobile and UDim2.new(1, 20, 0, 20) or UDim2.new(1, 20, 0, 20)
    
    local tween = TweenService:Create(notificationFrame, tweenInfo, {Position = targetPos})
    tween:Play()
    
    wait(5)
    
    local hideTween = TweenService:Create(notificationFrame, tweenInfo, {Position = hidePos})
    hideTween:Play()
    hideTween.Completed:Connect(function()
        notificationFrame.Visible = false
    end)
end

local function addButtonHover(button, hoverColor, normalColor)
    button.MouseEnter:Connect(function()
        local hoverTween = TweenService:Create(button, TweenInfo.new(0.2), {BackgroundColor3 = hoverColor})
        hoverTween:Play()
    end)
    
    button.MouseLeave:Connect(function()
        local normalTween = TweenService:Create(button, TweenInfo.new(0.2), {BackgroundColor3 = normalColor})
        normalTween:Play()
    end)
end

-- Add hover effects
addButtonHover(freezeBtn, Color3.fromRGB(0, 86, 179), Color3.fromRGB(0, 122, 255))
addButtonHover(forceAcceptBtn, Color3.fromRGB(30, 126, 52), Color3.fromRGB(40, 167, 69))
addButtonHover(minimizeBtn, Color3.fromRGB(255, 200, 60), Color3.fromRGB(255, 189, 46))
addButtonHover(closeBtn, Color3.fromRGB(255, 120, 110), Color3.fromRGB(255, 95, 87))

-- Button Events
freezeBtn.MouseButton1Click:Connect(function()
    spawn(function()
        showNotification()
    end)
end)

forceAcceptBtn.MouseButton1Click:Connect(function()
    spawn(function()
        showNotification()
    end)
end)

-- Window Controls
closeBtn.MouseButton1Click:Connect(function()
    screenGui:Destroy()
end)

minimizeBtn.MouseButton1Click:Connect(function()
    mainFrame.Visible = not mainFrame.Visible
end)

-- Make GUI draggable
local dragging = false
local dragStart = nil
local startPos = nil

titleBar.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 then
        dragging = true
        dragStart = input.Position
        startPos = mainFrame.Position
    end
end)

titleBar.InputChanged:Connect(function(input)
    if dragging and input.UserInputType == Enum.UserInputType.MouseMovement then
        local delta = input.Position - dragStart
        mainFrame.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
    end
end)

titleBar.InputEnded:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 then
        dragging = false
    end
end)

print("Trade Scam Gag GUI Loaded Successfully!")
